name: Keep Bot Alive

on:
  schedule:
    # يشغل المهمة كل 5 دقائق
    - cron: '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    
    steps:
      - name: Send request to bot
        run: |
          curl -X GET "https://your-bot-url.your-username.repl.co"
